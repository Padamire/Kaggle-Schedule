name: XGB Restart Scheduler

on:
  workflow_run:
    workflows: ["XGB Run Scheduler"]
    types:
      - completed

jobs:
  restart:
    runs-on: ubuntu-latest
    steps:
      - run: echo "XGB Run Scheduler completed, triggering restart..."
